const intialState = {
    status: 'L'
}

function user(state = intialState, action) {
    let { payload, type } = action
    switch (type) {
        default:
            return state
    }

}


export default user